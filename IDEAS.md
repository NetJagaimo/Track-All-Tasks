# Track-All-Tasks — 功能構想

一個隱私優先、純本地、靠快捷鍵與閒置偵測無痛記錄工時的 macOS 選單列 App。
**任務本身就是 todo item**：可計時、可完成封存、可丟回收桶。資料全部存在本機 SQLite，零雲端、免登入。

> 詳細設計定案見 [SPEC.md](SPEC.md)。本文件為功能概覽。

---

## 1. 核心計時功能 (time-tracking-core)

時間追蹤的引擎。

- 開始計時記下起始時間並進入「追蹤中」，停止時記下結束時間，時長由起訖即時計算（不儲存）。
- 每段計時都對應一個任務（= todo item），可手打或從「進行中」清單快選。
- 一次只能有一段計時；已在計時又按開始，先停舊的再開新的。
- 追蹤中時長秒級跳動更新。
- 開始計時即寫入一筆未結束紀錄，計時中每分鐘記一次心跳，供當機/睡眠復原。

## 2. 選單列介面 (menubar-ui)

App 常駐 macOS 狀態列，不出現在 Dock（`LSUIElement`）。

- 選單列圖示表示是否在計時；計時中顯示「圖示 + 任務名稱前 3 字 + 秒級時長」。
- 點圖示彈出 popover 面板（`.window` style）：目前狀態、開始/停止、任務名稱輸入（含進行中清單快選）、前往主視窗入口。
- 點面板外或 Esc 關閉，互動輕量不打斷工作。

## 3. 全域快捷鍵 (global-hotkeys)

不管焦點在哪都能操作（KeyboardShortcuts 套件，免輔助使用權限）。

- 「開始/停止」切換鍵：閒置時開始、計時中停止。
- 「任務輸入」鍵：彈出 popover 並把游標停在輸入欄。
- 皆可在設定自訂，改完即時生效、重開保留。

## 4. 閒置偵測 (idle-detection)

避免離座還在計時、灌水的紀錄。

- 輪詢滑鼠/鍵盤活動（`CGEventSource`，免權限），超過門檻沒動作就**當下自動停止**計時（不修剪、不提示）。
- 門檻可調（分鐘，預設 5，最少 1），可完全關閉。
- 自動停止後發系統通知提醒。

## 5. 任務 = Todo (task-todo)

任務是獨立資料表，同時是 todo item。

- 狀態：進行中 → 完成（兩步：勾完成仍在清單、再手動封存）→ 封存；任何狀態可丟回收桶。
- 只有「進行中」能計時；名稱唯一；可加備註；進行中清單可手動拖曳排序。
- 編輯紀錄的任務名 = 幫任務改名（連動所有紀錄）；撞名自動合併。
- 回收桶：任務與其紀錄一起進、一起還原、清空即永久刪。

## 6. 紀錄管理 (record-management)

手動補登與修改過去的紀錄。

- 手動新增：任務、起訖時間，自動算時長；結束早於開始會被擋下。
- 編輯：可改任務歸屬與時間範圍，改後重算時長。
- 刪除：確認後移除。

## 7. 查詢與匯出 (query-and-export)

日/週總覽與資料帶出。

- 每日總覽：當天紀錄依任務分組，列各任務小計與全天總計，可切換日期。
- 每週總覽：7 天彙整，**週一起算**；跨夜紀錄於半夜切開分攤到各日。
- 匯出：選日期區間，CSV 或 JSON（任務名稱、起訖、時長），時間用 ISO8601；跨夜匯出保持原始一筆。
- 範圍含封存任務、排除回收桶。

## 8. 本地儲存 (local-storage)

- 所有紀錄與設定存本機 SQLite（GRDB）；離線全功能可用。
- 免登入、免註冊，首次啟動即可開始。
- schema 版本管理與 migration 用 GRDB `DatabaseMigrator`。
- 資料層以 Repository 協定抽象（async + UUID + 純 struct），未來可換 Postgres / 遠端。

## 9. 系統整合 (system)

- 開機自動啟動開關（`SMAppService`）。
- 唯一系統授權：通知（第一次閒置自動停止時才請求）。
