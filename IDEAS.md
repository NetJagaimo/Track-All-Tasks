# Track-All-Tasks — 功能構想

一個隱私優先、純本地、靠快捷鍵與閒置偵測無痛記錄工時的 macOS 選單列 App。
資料全部存在本機 SQLite，零雲端、免登入。

---

## 1. 核心計時功能 (time-tracking-core)

時間追蹤的引擎。

- 開始計時時記下起始時間並進入「追蹤中」狀態，停止時記下結束時間並自動算出總時長。
- 每段計時都要有任務名稱，可手打或從最近用過的任務裡快選（顯示最近 10 個）。
- 一次只能有一段計時在進行；若已在計時又按開始，先停掉舊的再開新的。
- 追蹤中時長以時/分顯示，並至少每秒更新一次跳動。

## 2. 選單列介面 (menubar-ui)

App 常駐 macOS 狀態列，不出現在 Dock。

- 選單列圖示用外觀表示目前是否在計時；計時中會帶出目前任務名稱（過長就截斷，約 20 字）。
- 點圖示彈出一個 popover 面板：目前狀態、開始/停止按鈕、任務名稱輸入（含最近任務建議）、以及前往紀錄與設定的入口。
- 點面板外或按 Esc 即關閉。整體互動保持輕量、不打斷工作。

## 3. 全域快捷鍵 (global-hotkeys)

不管焦點在哪個 App 都能操作，免動滑鼠。

- 一組「開始/停止」快捷鍵切換計時：閒置時開始（或直接接續上一個任務），計時中則停止。
- 一組「任務輸入」快捷鍵：直接彈出 popover 並把游標停在任務輸入欄。
- 所有快捷鍵都能在設定裡自訂，改完立即生效，重開 App 後設定保留。

## 4. 閒置偵測 (idle-detection)

避免離開座位卻還在計時、灌水的紀錄。

- 監測滑鼠/鍵盤活動，超過設定的閒置門檻沒動作就自動停止目前計時。
- 門檻可調（以分鐘為單位，預設 5 分鐘，最少 1 分鐘）。
- 因閒置被自動停止後，使用者回來時會跳出提示，讓他選擇「保留」這段閒置時間，或「修剪」到最後一次有活動的時間點。
- 可以完全關閉閒置偵測。

## 5. 紀錄管理 (record-management)

手動補登與修改過去的紀錄。

- 手動新增：填任務名稱、起始與結束時間，系統建立紀錄並算出時長；結束早於開始會被擋下並提示錯誤。
- 編輯：可改任何既有紀錄的任務名稱與時間範圍，改時間後重新計算時長。
- 刪除：確認後永久移除紀錄。

## 6. 查詢與匯出 (query-and-export)

日/週總覽與資料帶出。

- 每日總覽：顯示當天所有紀錄，依任務分組，列出每個任務小計與全天總計；可切換到其他日期。
- 每週總覽：以 7 天為範圍，彙整每天、每個任務的時間。
- 匯出：可選日期區間，匯出成 CSV 或 JSON（含任務名稱、起始時間、結束時間、時長）。

## 7. 本地儲存 (local-storage)

- 所有紀錄與設定都存在 App 資料目錄下的 SQLite，完全不需要網路；斷網時所有功能照常運作。
- 不需要任何登入或註冊，首次啟動就能直接開始計時。
- 內建資料庫 schema 版本管理，App 更新若有結構變動會自動套用 migration。
